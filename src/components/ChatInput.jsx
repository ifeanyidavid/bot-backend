import React from 'react';

const ChatInput = () => (
  <div className="chat-input-wrapper">
    <header className="chat-input-header">
      <p>Reply</p>
    </header>
    <textarea className="chat-input" />
  </div>
);

export default ChatInput;
